import time
import sys


def main(amount, hours_elapsed):
    # get a set amount of listings between differernt time periods

    # make NFT objects out of each order

    # download each NFT (shuold do this iteratively, and ensure that you don't brech the limit --> wait one second between each request)

    pass

if __name__ == '__main__':
    amount = int(input('Amount'))
    hours_elapsed = int(input('Hours Elapsed: '))
    main(amount, hours_elapsed)
